VITE_GOOGLE_PLACE_ID=YOUR_GOOGLE_PLACE_ID
VITE_REVIEWS_ENDPOINT=/api/reviews

# If (and only if) you decide to call Google Places API directly from the browser (not recommended),
# you could expose a referrer‑restricted key like:
# VITE_GOOGLE_API_KEY=RESTRICTED_BROWSER_KEY
